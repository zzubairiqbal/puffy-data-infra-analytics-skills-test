# Part 3 : Business Analysis

This folder contains `executive-summary.pdf` and supporting analysis outputs.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r code/requirements.txt

python code/analyze.py --input-dir <part2_output_dir> --output-dir .
```
